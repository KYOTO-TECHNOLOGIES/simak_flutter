workflows:
  ios-workflow:
    name: iOS Workflow

    environment:
      flutter: stable

      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.simakfresh.app
        app_store_connect_key_name: Abhi_Codemagic


    scripts:
      - flutter pub get
      - flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa